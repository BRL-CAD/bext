diff -Naur MANIFOLD/CMakeLists.txt MANIFOLD_BLD/CMakeLists.txt
--- MANIFOLD/CMakeLists.txt	2026-04-30 12:43:33.539859541 -0400
+++ MANIFOLD_BLD/CMakeLists.txt	2026-04-30 12:48:59.647836994 -0400
@@ -36,6 +36,9 @@
   "${MANIFOLD_VERSION_MAJOR}.${MANIFOLD_VERSION_MINOR}.${MANIFOLD_VERSION_PATCH}"
 )
 
+# Toggle to also build a static library
+option(BUILD_STATIC_LIBS "Build a static library (manifold-static) alongside the shared library" OFF)
+
 # Primary user facing options
 option(MANIFOLD_CROSS_SECTION "Build CrossSection for 2D support" ON)
 option(MANIFOLD_DEBUG "Enable debug tracing/timing" OFF)
diff -Naur MANIFOLD/src/CMakeLists.txt MANIFOLD_BLD/src/CMakeLists.txt
--- MANIFOLD/src/CMakeLists.txt	2026-04-30 12:43:33.531859567 -0400
+++ MANIFOLD_BLD/src/CMakeLists.txt	2026-04-30 12:53:50.548912999 -0400
@@ -102,6 +102,60 @@
   PRIVATE ${MANIFOLD_INCLUDE_DIRS}
 )
 
+install(TARGETS manifold EXPORT manifoldTargets)
+
 exportlib(manifold)
 
-install(TARGETS manifold EXPORT manifoldTargets)
+# -----------------------------------------------------------------------
+# Optional static library target
+# -----------------------------------------------------------------------
+if(BUILD_STATIC_LIBS)
+  add_library(manifold_static STATIC
+    ${MANIFOLD_SRCS} ${MANIFOLD_PRIVATE_HDRS}
+  )
+
+  set_target_properties(manifold_static PROPERTIES
+    OUTPUT_NAME  manifold
+    EXPORT_NAME  manifold-static
+  )
+
+  target_compile_features(manifold_static PUBLIC cxx_std_17)
+  target_compile_options(manifold_static PRIVATE ${MANIFOLD_FLAGS})
+
+  # Reuse the OPTIONS list already set above for the shared target.
+  foreach(OPT IN LISTS OPTIONS)
+    if(${${OPT}})
+      target_compile_definitions(manifold_static PUBLIC ${OPT})
+    endif()
+  endforeach()
+  if(MANIFOLD_PAR)
+    target_compile_definitions(manifold_static PUBLIC MANIFOLD_PAR=1)
+  else()
+    target_compile_definitions(manifold_static PUBLIC MANIFOLD_PAR=-1)
+  endif()
+
+  target_include_directories(manifold_static
+    PUBLIC
+      $<INSTALL_INTERFACE:include>
+      $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>
+      $<BUILD_INTERFACE:${PROJECT_BINARY_DIR}/include>
+    PRIVATE
+      ${MANIFOLD_INCLUDE_DIRS}
+  )
+
+  target_link_libraries(manifold_static
+    PUBLIC
+      $<$<BOOL:${TRACY_ENABLE}>:TracyClient>
+    PRIVATE
+      $<$<BOOL:${MANIFOLD_CROSS_SECTION}>:Clipper2::Clipper2-static>
+      $<$<BOOL:${MANIFOLD_PAR}>:TBB::tbb>
+      $<$<BOOL:${MANIFOLD_EXPORT}>:assimp::assimp>
+  )
+
+  install(TARGETS manifold_static EXPORT manifoldTargets
+    ARCHIVE  DESTINATION ${CMAKE_INSTALL_LIBDIR}
+    INCLUDES DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
+  )
+
+endif()
+
diff -Naur MANIFOLD/src/vec.h MANIFOLD_BLD/src/vec.h
--- MANIFOLD/src/vec.h
+++ MANIFOLD_BLD/src/vec.h
@@ -19,6 +19,10 @@
 #define TracyAllocS(ptr, size, n) (void)0
 #define TracyFreeS(ptr, n) (void)0
 #endif
+#include <cstdlib>
+#include <limits>
+#include <memory>
+#include <new>
 #include <vector>

 #include "manifold/vec_view.h"
@@ -63,10 +67,9 @@
     this->capacity_ = this->size_;
     auto policy = autoPolicy(this->size_);
     if (this->size_ != 0) {
-      this->ptr_ = reinterpret_cast<T*>(malloc(this->size_ * sizeof(T)));
-      ASSERT(this->ptr_ != nullptr, std::bad_alloc());
-      TracyAllocS(this->ptr_, this->size_ * sizeof(T), 3);
-      copy(policy, vec.begin(), vec.end(), this->ptr_);
+      auto buffer = allocate(this->size_);
+      copy(policy, vec.begin(), vec.end(), buffer.get());
+      this->ptr_ = buffer.release();
     }
   }

@@ -75,10 +78,9 @@
     this->capacity_ = this->size_;
     auto policy = autoPolicy(this->size_);
     if (this->size_ != 0) {
-      this->ptr_ = reinterpret_cast<T*>(malloc(this->size_ * sizeof(T)));
-      ASSERT(this->ptr_ != nullptr, std::bad_alloc());
-      TracyAllocS(this->ptr_, this->size_ * sizeof(T), 3);
-      copy(policy, vec.begin(), vec.end(), this->ptr_);
+      auto buffer = allocate(this->size_);
+      copy(policy, vec.begin(), vec.end(), buffer.get());
+      this->ptr_ = buffer.release();
     }
   }

@@ -105,17 +107,8 @@

   Vec<T>& operator=(const Vec<T>& other) {
     if (&other == this) return *this;
-    if (this->ptr_ != nullptr) {
-      free_async(this->ptr_, capacity_);
-    }
-    this->size_ = other.size_;
-    capacity_ = other.size_;
-    if (this->size_ != 0) {
-      this->ptr_ = reinterpret_cast<T*>(malloc(this->size_ * sizeof(T)));
-      ASSERT(this->ptr_ != nullptr, std::bad_alloc());
-      TracyAllocS(this->ptr_, this->size_ * sizeof(T), 3);
-      manifold::copy(other.begin(), other.end(), this->ptr_);
-    }
+    Vec<T> replacement(other);
+    swap(replacement);
     return *this;
   }

@@ -160,16 +153,14 @@

   void reserve(size_t n) {
     if (n > capacity_) {
-      T* newBuffer = reinterpret_cast<T*>(malloc(n * sizeof(T)));
-      ASSERT(newBuffer != nullptr, std::bad_alloc());
-      TracyAllocS(newBuffer, n * sizeof(T), 3);
+      auto newBuffer = allocate(n);
       if (this->size_ > 0)
         manifold::copy(autoPolicy(this->size_), this->ptr_,
-                       this->ptr_ + this->size_, newBuffer);
+                       this->ptr_ + this->size_, newBuffer.get());
       if (this->ptr_ != nullptr) {
         free_async(this->ptr_, capacity_);
       }
-      this->ptr_ = newBuffer;
+      this->ptr_ = newBuffer.release();
       capacity_ = n;
     }
   }
@@ -200,23 +191,39 @@
   }

   void shrink_to_fit() {
-    T* newBuffer = nullptr;
+    Buffer newBuffer;
     if (this->size_ > 0) {
-      newBuffer = reinterpret_cast<T*>(malloc(this->size_ * sizeof(T)));
-      ASSERT(newBuffer != nullptr, std::bad_alloc());
-      TracyAllocS(newBuffer, this->size_ * sizeof(T), 3);
-      manifold::copy(this->ptr_, this->ptr_ + this->size_, newBuffer);
+      newBuffer = allocate(this->size_);
+      manifold::copy(this->ptr_, this->ptr_ + this->size_, newBuffer.get());
     }
     if (this->ptr_ != nullptr) {
       free_async(this->ptr_, capacity_);
     }
-    this->ptr_ = newBuffer;
+    this->ptr_ = newBuffer.release();
     capacity_ = this->size_;
   }

   size_t capacity() const { return capacity_; }

  private:
+  struct BufferDeleter {
+    void operator()(T* pointer) const noexcept {
+      TracyFreeS(pointer, 3);
+      free(pointer);
+    }
+  };
+  using Buffer = std::unique_ptr<T, BufferDeleter>;
+
+  static Buffer allocate(size_t count) {
+    if (count > std::numeric_limits<size_t>::max() / sizeof(T))
+      throw std::bad_alloc();
+    T* pointer = reinterpret_cast<T*>(malloc(count * sizeof(T)));
+    // Allocation failures must be handled in release builds as well.
+    if (pointer == nullptr) throw std::bad_alloc();
+    TracyAllocS(pointer, count * sizeof(T), 3);
+    return Buffer(pointer);
+  }
+
   size_t capacity_ = 0;

   static_assert(std::is_trivially_destructible<T>::value);
